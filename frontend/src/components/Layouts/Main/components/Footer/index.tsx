import { Box } from "@mui/system";

export const Footer = () => {
    return (
        <Box component="footer" sx={{
            flex: 1,
            backgroundColor: 'primary.main',
            height: 60,
            marginTop: 4
        }}>
            
        </Box>
    );
}